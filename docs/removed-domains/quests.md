# Quests

Quest and objective schemas, quest conditions, validation, and nested ID handling were removed because the active room-map model does not depend on quest tracking.

Quest-related UI controls remain dormant.

`TODO`: Reintroduce quests only after their runtime state, transitions, and persistence contract are defined.
