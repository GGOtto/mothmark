# Authored commands

The authored-command schema and branch resolver placeholder were removed. The small coded command runner remains because room traversal and room/feature preview use it; its command set is now limited to look, examine, movement, and help.

Command-authoring UI controls remain dormant.

`TODO`: Rebuild authored commands around room and feature targeting before widening their entity scope.
